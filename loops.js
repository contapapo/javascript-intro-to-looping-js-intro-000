function forLoop(){
  for (var i = 0; i <= 25; i++) {
    var resp=[];
    if (i <== 1) {
      resp.push (`I am ${i} strange loop.`);

    }
    else {
      resp.push (`I am ${i} strange loops.`);

    }
return resp;
}
}
